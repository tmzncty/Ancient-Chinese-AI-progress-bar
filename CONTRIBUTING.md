# Contributing

这个仓库不是模型榜单，也不是“古文 AI 已完成多少百分比”的宣传页。

它的目标是持续回答一个更窄、也更可检验的问题：

> **在某一种古代文献材料、某一个明确任务上，机器现在可靠到了什么程度？**

因此，任何修改 `progress.yaml` 中能力等级的 PR，都应尽量留下可复核依据。

## 1. 提交一条进度更新时，请写清楚四件事

### 1.1 Task：到底在测什么？

好的描述：

- 明代刻本文字 OCR；
- 二十四史人名/地名 NER；
- 未标点史部文本的句读；
- 给定版本全文库后的出处约束问答。

不够好的描述：

- 古文理解；
- AI 读古书；
- 大模型变强了。

### 1.2 Material：材料边界是什么？

至少考虑：

- 时代；
- 文体 / 部类；
- 刻本、抄本、碑刻或现代录文本；
- 繁简与异体字处理；
- 是否已经人工标点；
- 是否包含未公开测试集。

同一个模型在正史、诗歌、医籍、地方志上的表现可以差很多，因此不能用一个数据集代表全部古文。

### 1.3 Evidence：证据是什么？

优先级大致如下：

1. 共享任务 / 公共 benchmark，且测试集与评测脚本明确；
2. 同行评审论文中可复核的实验；
3. 开源数据 + 开源代码 + 可复现实验；
4. 大规模、持续的真实工作流记录；
5. 公开 demo；
6. 个别截图 / 主观体验。

第 5、6 类可以记录，但通常不足以单独把能力升到 3 级以上。

### 1.4 Failure：它在哪里失败？

每次“进步”最好同时记录至少一种失败：

- OCR 的罕见字 / 污损 / 行序错误；
- 标点时改动了原字；
- NER 在医籍上显著下降；
- 翻译补出了原文没有的主语；
- 问答给出合理但不存在的出处；
- 校勘忽略版本谱系而只按语言概率选字。

**如果只看到成功案例，看不到失败边界，就还不足以判断进步。**

---

## 2. 等级如何调整

### Level 0 — `░░░░░`

尚未形成可重复方案。

### Level 1 — `█░░░░`

已经有 demo、零散实验或少数成功案例，但泛化能力未知。

### Level 2 — `██░░░`

已经有公开数据、模型、工具或 benchmark，说明问题可以被系统研究；但真实使用中仍有大量人工修正。

### Level 3 — `███░░`

在**限定材料与工作流**中已经有明显实用价值，机器可以完成初稿、预标注或候选发现，但结果仍需要人工复核。

### Level 4 — `████░`

在边界明确的场景中，可以作为常规生产/研究工具使用；人工更多负责疑难项和抽查，而不是逐条重做。

### Level 5 — `█████`

高度成熟，可以大规模自动化，而且错误类型、质量控制方法和适用边界已经非常清楚。

> 5 级不是“像人”，而是“这个任务已经工程成熟”。

---

## 3. 不要把这些东西混在一起

### 识别原文 vs 规范化文本

OCR 得到“某个异体字”是一件事，把它映射到现代规范字是另一件事。建议始终保留原始转录层。

### 自动标点 vs 恢复原标点

多数古籍原本并没有现代标点。自动标点通常是在提出一种现代整理方案，而不是恢复某个唯一答案。

### 语言概率 vs 文献证据

模型认为某个字“更顺”，不等于这个异文“更正确”。版本、讹误机制、避讳、引用关系、时代用例都可能比语言流畅度更重要。

### 生成解释 vs 找到依据

研究辅助优先追求：

```text
结论 -> 原文 -> 版本 -> 页/卷/条目 -> 可复核来源
```

而不是：

```text
问题 -> 一段听起来很懂的回答
```

---

## 4. `progress.yaml` 约定

每个能力项至少包含：

```yaml
- id: stable-machine-readable-id
  domain: text-structure
  name_zh: 断句与标点
  level: 3
  confidence: high
  rationale: >-
    Why this level, including the practical boundary.
  evidence:
    - source-id
```

`confidence` 表示**我们对这个等级判断本身有多大把握**，不是模型置信度。

- `high`：有直接、较新的共享评测或多项独立证据；
- `medium`：有较好证据，但材料范围、版本或实际工作流覆盖有限；
- `low`：主要根据相邻任务、工具状态或少量公开实践估计。

如果没有足够证据，宁可保持低等级 + 低 confidence，也不要为了“进度条好看”升级。

---

## 5. 推荐的 PR 标题

```text
progress: recalibrate punctuation after EvaHan 2024
progress: add 2026 handwritten OCR evidence
sources: add new Classical Chinese benchmark
method: split variant recognition from normalization
```

一个 PR 最好只改变一组有共同证据来源的判断。

---

## 6. 什么内容很欢迎

- 新的古文 / 古籍 benchmark；
- 共享评测结果；
- 可公开的数据集；
- OCR、版面分析、NER、句法分析、翻译、检索工具；
- 文献学者的真实使用记录；
- **失败案例**；
- 对本仓库某一等级的反例；
- 能说明“模型看起来会，其实并不会”的测试。

尤其欢迎最后三类。这个项目真正有价值的部分，不是一直把格子涂满，而是把边界画清楚。
